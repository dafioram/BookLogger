# BookTracker Configuration

# The port you will access in the browser (e.g., http://raspberrypi:8000)
PORT=8000

# Timezone for correct backup timestamps
TZ=America/New_York

# App Secret Key (for sessions/security later)
SECRET_KEY=change_this_to_something_random